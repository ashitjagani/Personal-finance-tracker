import React from "react";

const Formgroupchild=({name, email, password, confirmpassword})=>{
    console.log(name, email,password,confirmpassword);
    return (
        <div>
     
        </div>
    )
}

export default Formgroupchild;